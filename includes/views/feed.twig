<div class="fm-alert post-queue-alert"></div>

<div class="fm-posts" data-ids="{{ post_ids }}" data-pinned="{{ post_pinned }}">

    {% for i, post in posts %}

      {% include "stub.twig" %}

    {% endfor %}

</div>

{{ nonce }}
